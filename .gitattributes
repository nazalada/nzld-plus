# Дилиб лежит в репозитории УЖЕ СОБРАННЫМ и подписанным — пользователю не нужен Xcode.
# binary обязателен: любая нормализация переносов строк порвёт подпись, и dyld молча
# откажется грузить библиотеку.
*.dylib binary
